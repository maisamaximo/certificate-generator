# certificate-generator
A simple Angular application for generating customizable certificates. It provides a default template where users can edit the content, preview the result, and export the final certificate through a clean and efficient interface.
